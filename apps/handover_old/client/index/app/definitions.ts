export type Layers = typeof layers;
export const layers = {
	page: {
		root: {
			home:     { },
			settings: { },
		},
	},
};


export type Scopes = typeof scopes;
export const scopes = {
	root: {
		home:     { },
		settings: { },
	},
};
