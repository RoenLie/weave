/**
 * Current load location for the closest inversify scope in use.
 */
export const $LoadLocation = Symbol.for('lit-aegis:loadLocation');

/**
 * Identifier for the container instance closest to the current inversify injectable scope.
 */
export const $Container = Symbol.for('lit-aegis:container');
