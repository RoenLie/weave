name: Publish image-viewer-demo

on:
  # Runs on pushes targeting the default branch
  push:
    paths:
      - "components/image-viewer/**"
    branches: ["publish"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  publish-npm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: latest
          registry-url: https://registry.npmjs.org/

      - name: Setup pnpm
        uses: pnpm/action-setup@v3.0.0
        with:
          version: latest

      - name: Install dependencies.
        run: pnpm install

      - name: Build dependencies and project.
        run: |
          pnpm build @roenlie/image-viewer
          pnpm --filter @roenlie/image-viewer build-demo

      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy components/image-viewer/dist/demo --project-name=image-viewer-cmp


